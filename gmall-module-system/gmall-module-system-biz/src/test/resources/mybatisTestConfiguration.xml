<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>


    <!-- 配置数据源 -->
    <properties>
        <property name="jdbc.driver" value="com.mysql.cj.jdbc.Driver"/>
        <property name="jdbc.url" value="jdbc:mysql://localhost:3306/gmall_sys"/>
        <property name="jdbc.username" value="root"/>
        <property name="jdbc.password" value="root"/>
    </properties>

    <!-- 配置 MyBatis 设置 -->
    <settings>
        <!-- 这里可以配置一些 MyBatis 的全局设置，例如开启延迟加载、缓存等 -->
        <setting name="cacheEnabled" value="true"/>
        <setting name="lazyLoadingEnabled" value="false"/>
        <setting name="logImpl" value="STDOUT_LOGGING"/>
    </settings>


    <!-- 配置类型别名 -->
    <typeAliases>
        <typeAlias alias="AdminUser" type="com.zp.gmall.module.system.entity.AdminUserDO"/>
    </typeAliases>

    <!-- 配置 MyBatis 环境 -->
    <environments default="default">
        <environment id="default">
            <transactionManager type="JDBC"/>
            <dataSource type="UNPOOLED">
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="jdbc:mysql://127.0.0.1:3306/gmall_sys"/>
                <property name="username" value="root"/>
                <property name="password" value="root"/>
            </dataSource>
        </environment>
    </environments>


    <!-- 配置 MyBatis 的映射器 -->
    <mappers>
        <mapper resource="mapper/system/AdminUserMapper.xml"/>
    </mappers>

</configuration>

